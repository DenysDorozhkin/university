export enum menuActiveEnum {
  STUDENTS = "students",
  LECTORS = "lectors",
  COURSES = "courses",
  GROUPS = "groups",
}

export enum headerTitleEnum {
  STUDENTS = "Students",
  LECTORS = "Lectors",
  COURSES = "Courses",
  GROUPS = "Groups",
}
