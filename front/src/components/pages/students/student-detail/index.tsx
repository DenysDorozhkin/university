import { FC } from "react";
import StudentDetailLayout from "../../../layouts/dashboard/student-detail";

const StudentDetailPage: FC = () => {
  return <StudentDetailLayout />;
};

export default StudentDetailPage;
